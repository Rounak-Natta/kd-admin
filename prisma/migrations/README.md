# Database migrations

The POS repository (`kd-app`) is the single owner of the shared PostgreSQL migration chain. Run migrations from the POS project. The Admin app never resets or migrates the shared schema.
